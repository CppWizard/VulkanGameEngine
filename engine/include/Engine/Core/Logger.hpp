#pragma once

#include <iostream>

#pragma once

#include <iostream>

#define ENGINE_INFO(x)  std::cout << "[Info] "  << x << '\n'
#define ENGINE_WARN(x)  std::cout << "[Warn] "  << x << '\n'
#define ENGINE_ERROR(x) std::cerr << "[Error] " << x << '\n'